<Window x:Class="PocketDrop.ShortcutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Text_EditShortcutTitle}" Height="360" Width="480"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        FontFamily="Segoe UI Variable, Segoe UI, sans-serif"
        Loaded="Window_Loaded" KeyDown="Window_KeyDown">

    <Border Background="{DynamicResource LayerDefault}" BorderBrush="{DynamicResource StrokeCardDefault}" BorderThickness="1" CornerRadius="8" Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="4" Opacity="0.15" Direction="270" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="24,24,24,0">
                <TextBlock x:Name="TitleText" Text="New shelf shortcut" FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,10"/>
                <TextBlock Text="{DynamicResource Text_PressLetterKey}" FontSize="14" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,40"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Border Background="#005FB8" CornerRadius="4" Padding="14,10" Margin="0,0,6,0" MinWidth="48">
                        <Path Data="M3 3H11V11H3V3ZM13 3H21V11H13V3ZM3 13H11V21H3V13ZM13 13H21V21H13V13Z" Fill="White" Width="16" Height="16" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <Border Background="#005FB8" CornerRadius="4" Padding="14,10" Margin="0,0,6,0" MinWidth="48">
                        <TextBlock Text="Shift" Foreground="White" FontWeight="SemiBold" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <Border Background="#005FB8" CornerRadius="4" Padding="14,10" MinWidth="48">
                        <TextBlock x:Name="TargetKeyText" Text="Z" Foreground="White" FontWeight="SemiBold" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </StackPanel>

                <TextBlock Text="{DynamicResource Text_ModifiersLocked}" FontSize="12" Foreground="{DynamicResource TextSecondary}" Margin="0,45,0,0"/>
            </StackPanel>

            <Border Grid.Row="1" Background="{DynamicResource LayerAlt}" BorderBrush="{DynamicResource StrokeControlDefault}" BorderThickness="0,1,0,0" CornerRadius="0,0,8,8" Padding="24,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Content="{DynamicResource Text_Save}" Click="Save_Click" Foreground="White" Background="#005FB8" BorderThickness="0" Padding="0,8" Margin="0,0,8,0" Cursor="Hand" FontSize="14" FontWeight="SemiBold">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button Grid.Column="1" Content="{DynamicResource Text_Cancel}" Click="Cancel_Click" Foreground="{DynamicResource TextPrimary}" Background="{DynamicResource LayerDefault}" BorderBrush="{DynamicResource StrokeControlDefault}" BorderThickness="1" Padding="0,8" Margin="8,0,0,0" Cursor="Hand" FontSize="14" FontWeight="SemiBold">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>